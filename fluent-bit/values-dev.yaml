fluent-bit:
  config:
    ## https://docs.fluentbit.io/manual/pipeline/outputs
    outputs: |
      [OUTPUT]
          name                   loki
          match                  *
          host                   loki-headless
          port                   3100
          labels                 job=fluentbit-k8s
          # auto_kubernetes_labels on
